<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Startup_Verdelicia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/colorButtonGreen</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorButtonNormal">@color/colorButtonGreen</item>
    </style>

    <!-- Derivando o tema principal do base -->
    <style name="Theme.Startup_Verdelicia" parent="Base.Theme.Startup_Verdelicia" />
</resources>
